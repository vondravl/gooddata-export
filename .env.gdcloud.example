# GoodData Export Configuration
# Copy this file to .env.gdcloud and fill in your values

# ====================
# Required Settings
# ====================

# GoodData instance URL (required)
BASE_URL=https://your-instance.cloud.gooddata.com

# Workspace ID to export (required)
WORKSPACE_ID=your_workspace_id

# API authentication token (required)
BEARER_TOKEN=your_bearer_token_here

# ====================
# Optional Settings
# ====================

# Child Workspace Processing
# ---------------------------
# Include child workspaces in export
# CLI equivalent: --include-child-workspaces
INCLUDE_CHILD_WORKSPACES=false

# Comma-separated list of data types to fetch from child workspaces (default: dashboards)
# Options: metrics, dashboards, visualizations, filter_contexts
# CLI equivalent: --child-workspace-data-types
# CHILD_WORKSPACE_DATA_TYPES=dashboards,visualizations

# Maximum parallel workers for child workspace processing (default: 5)
# CLI equivalent: --max-workers
MAX_WORKERS=5

# Feature Flags
# -------------
# Enable extraction from rich text widgets (default: true for single workspace, false for multi-workspace)
# CLI equivalent: --enable-rich-text-extraction
ENABLE_RICH_TEXT_EXTRACTION=true

# Enable post-export enrichment: views, updates, procedures (default: true)
# CLI equivalent: --skip-post-export (inverts this - use flag to disable enrichment)
ENABLE_POST_EXPORT=true

# Enable debug logging for workspace processing
# CLI equivalent: --debug
DEBUG=false

# ====================
# Notes
# ====================
# 
# Precedence (highest to lowest):
#   1. CLI arguments (explicit user intent)
#   2. .env.gdcloud settings (project configuration)
#   3. Code defaults (fallback)
#
# Example CLI overrides:
#   python main.py export --max-workers 10  (overrides MAX_WORKERS)
#   python main.py export --skip-post-export  (disables ENABLE_POST_EXPORT)
#
# Note: CLI uses dashes (--max-workers) while env vars use underscores (MAX_WORKERS)
# This is standard practice: CLI follows POSIX, env vars follow shell conventions
#
# Currently NOT configurable via .env:
#   - --db-dir, --csv-dir (output paths)
#   - --db-name (custom database filename)
#   - --format (export format selection)

